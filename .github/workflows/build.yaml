name: Build plugin binary

on:
  push:
    branches:
      - main

jobs:
  build-binary:
    name: Build plugin binary
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Build binary
        run: make
